# Crash Simulation ETL

This example demonstrates a Source → Filter → Sink pipeline for curating LS-DYNA crash simulation data.
Automotive crash simulations produce multi-timestep shell meshes stored in the `d3plot` binary format.
The pipeline reads these files, removes non-deforming wall nodes, logs mesh metadata, converts fields
to single precision, and writes the processed meshes to disk.

## Prerequisites

```bash
pip install physicsnemo-curator[mesh] lasso-python
```

## Input Data

This pipeline expects LS-DYNA crash simulation data organized as run directories,
each containing a `d3plot` binary file and an optional `.k` keyword file:

```text
input/crash_simulations/
├── Run100/
│   ├── d3plot          # Required: binary mesh/displacement data
│   └── run100.k        # Optional: part thickness definitions
├── Run101/
│   ├── d3plot
│   └── run101.k
└── ...
```

### d3plot File (Required)

LS-DYNA binary format containing:

- Node coordinates (reference configuration at t=0)
- Node displacements over time (temporal trajectory)
- Shell element connectivity (triangles and/or quads)
- Part IDs for each element
- Multiple timesteps of deformation data

### .k File (Optional)

LS-DYNA keyword format containing:

- `*PART` definitions linking parts to sections
- `*SECTION_SHELL` definitions with thickness values

If no `.k` file is found, node thickness defaults to 0 for all nodes.

### Obtaining Data

LS-DYNA crash simulation data (d3plot files) is typically generated by running
LS-DYNA finite element solvers on automotive crash models. If you do not have
your own simulation data, place any LS-DYNA d3plot output into the expected
directory structure above.

For model training after data curation, see the
[PhysicsNeMo Crash Example](https://github.com/NVIDIA/physicsnemo/blob/main/examples/structural_mechanics/crash/README.md)
which documents the full training workflow using curated VTP or Zarr outputs.

## Usage

```bash
python main.py
```

By default, the pipeline reads from `input/crash_simulations/` and writes
processed meshes to `output/crash_simulations/`. Edit `INPUT_DIR` and
`OUTPUT_DIR` in `main.py` to point to your data locations.

## Pipeline Steps

1. **D3PlotSource** — Reads d3plot binary files (with optional stress and .k file parsing)
2. **WallNodeFilter** — Removes non-deforming wall nodes (threshold=1.0)
3. **MeshInfoFilter** — Logs mesh metadata to a JSON-lines summary
4. **PrecisionFilter** — Converts fields to float32
5. **MeshSink** — Writes processed meshes as TensorDict memory-mapped directories

## See Also

- [Crash Data Processing Reference][crash-ref] — Detailed data format specifications
- [PhysicsNeMo Crash Training Example][crash-train] — Model training guide
- [PhysicsNeMo-Curator Documentation][curator] — General ETL framework documentation

[crash-ref]: https://github.com/NVIDIA/physicsnemo-curator/blob/main/examples/structural_mechanics/crash/Crash_Data_Processing_Reference.md
[crash-train]: https://github.com/NVIDIA/physicsnemo/blob/main/examples/structural_mechanics/crash/README.md
[curator]: https://github.com/NVIDIA/physicsnemo-curator
